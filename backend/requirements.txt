fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
jsonschema==4.23.0
# NATS client (modern; the old `asyncio-nats-client` is deprecated)
nats-py==2.9.0
sqlalchemy[asyncio]==2.0.36
# Async PostgreSQL driver — required because DATABASE_URL uses postgresql+asyncpg://
asyncpg==0.29.0
# Sync PG driver (kept only for Alembic offline / scripts; remove if unused)
psycopg2-binary==2.9.9
alembic==1.13.3
python-multipart==0.0.12
httpx==0.27.2
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-requests==0.48b0
opentelemetry-exporter-otlp==1.27.0
prometheus-client==0.21.0
pyotp==2.9.0
# QR code generation. pyqrcode does not expose a `pil` extra; install Pillow + pypng explicitly.
pyqrcode==1.2.1
pypng==0.20220715.0
Pillow==11.0.0
redis==5.1.1
cryptography==43.0.1
geoalchemy2==0.15.2
numpy==1.26.4
scikit-learn==1.5.2
# Utilidades
python-dotenv==1.0.1
# NOTE: `asyncio` is in the Python stdlib — never list it here.
# NOTE: ML pesado (ultralytics, transformers, torch, networkx) movido a requirements-ml.txt
